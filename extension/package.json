{
  "name": "extension",
  "version": "1.0.0",
  "description": "金沢大学ポータルおよびWebClass LMSから履修情報・課題情報を取得し、KU Calendarに同期するChrome拡張機能。",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/generate-manifest.js production && BUILD_ENTRY=background vite build && BUILD_ENTRY=content vite build && BUILD_ENTRY=popup vite build && BUILD_ENTRY=appContent vite build && cp manifest.json dist/ && cp src/popup/popup.html dist/ && cp src/page-bridge.js dist/ && cp -r icons dist/",
    "build:dev": "node scripts/generate-manifest.js development && BUILD_ENTRY=background vite build --mode development && BUILD_ENTRY=content vite build --mode development && BUILD_ENTRY=popup vite build --mode development && BUILD_ENTRY=appContent vite build --mode development && cp manifest.json dist/ && cp src/popup/popup.html dist/ && cp src/page-bridge.js dist/ && cp -r icons dist/",
    "manifest:dev": "node scripts/generate-manifest.js development",
    "manifest:prod": "node scripts/generate-manifest.js production",
    "dev:background": "BUILD_ENTRY=background vite build --watch",
    "dev:content": "BUILD_ENTRY=content vite build --watch",
    "dev:popup": "BUILD_ENTRY=popup vite build --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "@types/chrome": "^0.1.43",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "tailwindcss": "^4.3.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
